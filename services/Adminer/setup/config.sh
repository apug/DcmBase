#!/bin/sh
# Adminer is stateless - no configuration needed
printf '%b\n' "--- Adminer Configuration ---" >&2
printf '%b\n' "Adminer requires no configuration (uses database from other services)" >&2
printf '%b\n' "" >&2
